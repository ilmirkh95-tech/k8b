apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: ilmir
  labels:
    app: ilmir
spec:
  replicas: 3
  selector:
    matchLabels:
      app: ilmir
  template:
    metadata:
      labels:
        app: ilmir
    spec:
      containers:
        - name: kuard
          image: jmutai/kuard-amd64:latest
          ports:
            - containerPort: 8080
              name: http
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /healthy
              port: 8080
            initialDelaySeconds: 15
            timeoutSeconds: 2
            periodSeconds: 20
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /ready
              port: 8080
            initialDelaySeconds: 5
            timeoutSeconds: 1
            periodSeconds: 5
            failureThreshold: 3
            successThreshold: 1
